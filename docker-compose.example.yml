version: '3'

services:
  postgres:
    container_name: profico-web
    image: profico-web:${VERSION}
    hostname: profico-web
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=production
    restart: always
    command: yarn start
